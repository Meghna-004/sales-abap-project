@EndUserText.label: 'Sales Data CDS View for Reporting'
@AccessControl.authorizationCheck: #NOT_REQUIRED

define view entity ZC_SALES
  as select from zsales
{
  key sales_id,
      customer_name,
      product_name,
      region,
      sales_type,
      quantity,
      price
}
